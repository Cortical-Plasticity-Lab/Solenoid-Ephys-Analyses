function E = JMP(T,varargin)
%JMP Convert table to format for JMP export and write excel table
%
%  tbl.stats.JMP(T);
%  E = tbl.stats.JMP(T,...);
%
% Inputs
%  T        - Table exported using solRat.makeTables
%  varargin - (Optional) pairs of 'Name', value inputs
%
% Output
%  E        - Converted output table that was written to file

end